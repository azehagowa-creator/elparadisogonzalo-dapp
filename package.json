{
  "name": "elparadisogonzalo",
  "version": "1.0.0",
  "public": true,
  "description": "El Paradiso Gonzalo website and dashboard",
  "homepage": "https://elparadisogonzalo.net",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && rollup -c && cp index.html dist/ && cp -r assets dist/",
    "deploy": "pnpm build && pnpm exec wrangler deploy"
  },
  "devDependencies": {
    "crx3": "^2.0.0",
    "rollup": "^4.63.4",
    "typescript": "^7.0.2",
    "wrangler": "^4.136.3"
  }
}
